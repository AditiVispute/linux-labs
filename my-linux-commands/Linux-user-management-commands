# Linux User Management Commands (Practical)

This section includes basic commands used to manage users in a Linux system.

---

## useradd

Use:
The useradd command is used to create a new user account in the system.

Command:
```bash
useradd user1

Sample Output:
User created successfully

Creates a new user with default settings.

## passwd

 Use:
  Used to set or change the password of a user.

 Command:
  passwd user1

 Sample Output:
  Password updated successfully

 Prompts to enter a new password for the user

## userdel
Use:
  Used to delete a user account from the system.

Command:
  userdel user1

Sample Output:
  User deleted successfully

Removes the specified user from the system.

## whoami
 Use:
   Displays the name of the currently logged-in user.

 Command:
   whoami

 Sample Output:
   aditi

 Helps in identifying the current user account.
